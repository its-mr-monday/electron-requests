const { ipcMain } = require('electron');
const axios = require('axios');
const https = require('https');

const initializeBindings = (verifyHttpCertificates = true) => {

}

module.exports = {
    initializeBindings
}